alias vim="nvim"

alias systeminfo="inxi -SPARM -GCDN -v1 -xGCRS"

alias ls="eza"

alias zen="app.zen_browser.zen"

alias fzfp="fzf --preview 'bat --style=numbers --color=always {}'"

alias init-nvm="source /usr/share/nvm/init-nvm.sh"

alias refresh-mirrorlist="sudo reflector --protocol http,https --country US,Brazil,Paraguay --latest 50 --sort rate --save /etc/pacman.d/mirrorlist"
